export interface AuthModel {
    display: string
    email: string
    expirationDate: Date
    id: string
    isValid: boolean
    pictureUrl: string
    refreshToken: string
    roleId: number
    token: string
    userId: number
    userName: string
}
